# Lab 6

## Tasks

- Task 1: Filtering in the frequency domain using the Laplacian filter.
- Assessment Task 1: Apply Sobel filter(s) in the frequency domain using the same cameraman image.

## Notes

- The notebook uses the same `cameraman.tif` image style as the earlier labs.
- Open and run `lab6.ipynb` inside the `lab6` folder.
